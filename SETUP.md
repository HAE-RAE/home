# 아래아 지원 페이지 — 설정 가이드

`apply.html` 은 서버가 전혀 필요 없는 정적 페이지입니다.
지원서 제출은 **여러분이 만든 Google Form으로 조용히 전송**되고, 응답은 Google Sheet에 쌓입니다.
지원자는 Google 화면을 전혀 보지 않고, 로그인도 필요 없습니다.

준비 시간: **약 15분**

---

## 1단계 — Google Form 만들기

[forms.google.com](https://forms.google.com) 에서 새 양식을 만들고, 아래 14개 질문을 **순서대로, 아래 유형 그대로** 추가하세요.

> ⚠️ **중요 — 반드시 지켜야 하는 3가지**
> 1. 모든 질문을 **단답형** 또는 **장문형**으로 만드세요. 객관식/체크박스로 만들면 안 됩니다.
>    (선택지 UI는 웹페이지가 이미 그려줍니다. Google Form은 단순한 저장소 역할만 합니다.
>    객관식으로 만들면 값이 조금만 달라도 제출이 실패합니다.)
> 2. **어떤 질문도 '필수'로 설정하지 마세요.** 필수 항목이 비면 제출이 조용히 실패합니다.
>    필수 여부 검증은 웹페이지가 이미 하고 있습니다.
> 3. 설정 → 응답 → **'이메일 주소 수집'을 끄세요.** 켜두면 지원자에게 Google 로그인을 요구합니다.

| # | 질문 제목 (그대로 복사) | 유형 |
|---|---|---|
| 1 | 이름 | 단답형 |
| 2 | 이메일 | 단답형 |
| 3 | 소속 · 전공 · 학년 | 단답형 |
| 4 | 링크 | 장문형 |
| 5 | 파이썬 경험 | 단답형 |
| 6 | 영어 자료를 다루는 정도 | 단답형 |
| 7 | 써본 적 있는 것 | 장문형 |
| 8 | [서술1] 스스로 시작해서 끝까지 완주한 일 | 장문형 |
| 9 | [서술2] 최근 흥미롭게 본 AI · NLP 주제 | 장문형 |
| 10 | 주당 참여 가능 시간 | 단답형 |
| 11 | 앞으로 1년 사이 예정된 일정 | 단답형 |
| 12 | 알게 된 경로 | 단답형 |
| 13 | 더 하고 싶은 말 | 장문형 |
| 14 | 개인정보 수집·이용 동의 | 단답형 |

---

## 2단계 — FORM_ID 찾기

양식 우측 상단 **보내기 → 🔗 링크** 를 누르면 이런 주소가 나옵니다.

```
https://docs.google.com/forms/d/e/1FAIpQLSd_XXXXXXXXXXXXXXXXXXXX/viewform
                                  └──────────── 이 부분이 FORM_ID ────────────┘
```

`1FAIpQLSd...` 로 시작하는 긴 문자열을 복사하세요.

---

## 3단계 — entry ID 14개 찾기

가장 쉬운 방법은 **미리 채워진 링크**입니다.

1. 양식 편집 화면 우측 상단 **⋮ (점 세 개) → 미리 채워진 링크 가져오기**
2. 14개 칸에 **각각 다른 아무 값**을 넣으세요. 헷갈리지 않게 `1`, `2`, `3` … `14` 를 추천합니다.
3. 아래 **링크 가져오기** → **링크 복사**
4. 복사한 주소를 메모장에 붙여넣으면 이렇게 생겼습니다.

```
...viewform?usp=pp_url&entry.1234567890=1&entry.9876543210=2&entry.5555500000=3 ...
             질문1 ↑                    질문2 ↑                 질문3 ↑
```

값 `1`이 붙은 것이 1번 질문의 entry ID, `2`가 붙은 것이 2번 질문의 entry ID … 순서대로 대응됩니다.

---

## 4단계 — HTML에 붙여넣기

`apply.html` 을 텍스트 편집기로 열고, 파일 아래쪽 `CONFIG` 블록을 찾아 채우세요.
(`Ctrl+F` / `Cmd+F` 로 `PASTE_YOUR_FORM_ID_HERE` 검색)

```js
const CONFIG = {
  FORM_ID: "1FAIpQLSd_여기에_붙여넣기",

  ENTRY: {
    name:    "entry.1234567890",   // 1번 질문
    email:   "entry.9876543210",   // 2번
    affil:   "entry.5555500000",   // 3번
    links:   "entry.____",         // 4번
    python:  "entry.____",         // 5번
    english: "entry.____",         // 6번
    tools:   "entry.____",         // 7번
    essay1:  "entry.____",         // 8번
    essay2:  "entry.____",         // 9번
    hours:   "entry.____",         // 10번
    plans:   "entry.____",         // 11번
    source:  "entry.____",         // 12번
    extra:   "entry.____",         // 13번
    consent: "entry.____"          // 14번
  }
};
```

`FORM_ID` 를 채우는 순간 페이지 상단의 **주황색 운영자 안내 배너는 자동으로 사라집니다.**
따로 지울 필요 없습니다.

---

## 5단계 — 내용 채우기 (필수)

페이지에 **주황색 점선 밑줄**로 표시된 자리가 아직 비어 있습니다. `Ctrl+F` 로 `todo` 를 검색하면 전부 찾을 수 있습니다.

- 모집 마감일 (히어로 영역 + 일정 카드 + 제출 완료 화면, 총 3곳)
- 서류 발표 / 면접 / 최종 발표 / 활동 시작 일정
- 모집 인원
- 온라인 · 오프라인 여부와 지역
- 참가비 (무료라면 "무료"라고 명시하는 편이 좋습니다 — 안 적혀 있으면 유료라고 넘겨짚습니다)
- 문의 이메일 (본문 1곳 + 푸터 1곳)

---

## 6단계 — 테스트

1. 브라우저에서 `apply.html` 을 열고 본인이 직접 지원서를 한 번 제출해 보세요.
2. Google Form의 **응답** 탭에 값이 들어왔는지, 14개 칸이 **엉뚱한 자리에 들어가지 않았는지** 확인하세요.
   자리가 밀렸다면 3단계의 entry ID 순서가 어긋난 것입니다.
3. 응답 탭에서 **📊 Sheets에 연결** 을 눌러 스프레드시트로 받으면 심사가 훨씬 편합니다.
4. 확인이 끝나면 테스트 응답은 삭제하세요.

---

## 7단계 — 배포

배포는 사이트 폴더 전체를 올리는 방식입니다. 자세한 절차와 커스텀 도메인 연결은
같은 폴더의 **`README.md`** 를 참고하세요. 요약하면:

1. `HAE-RAE/home` 저장소의 내용을 이 폴더의 파일들로 교체
2. **Settings → Pages → Source: `main` / `/root`**
3. 도메인이 있다면 저장소 루트에 `CNAME` 파일을 만들고 도메인만 한 줄 적기

> 저장소가 public이어도 **지원서 내용은 공개되지 않습니다.** 응답은 GitHub이 아니라
> 여러분의 Google Sheet에만 저장됩니다. 공개되는 것은 페이지 코드뿐입니다.

---

## 알아두면 좋은 것

**마감 후 지원 차단** 은 Google Form 쪽에서 **설정 → 응답 → '응답 받기' 끄기** 로 처리하세요.
페이지는 그대로 두고 히어로 문구만 "모집이 마감되었습니다"로 바꾸면 됩니다.

**제출 실패 시** 페이지는 조용히 성공 화면을 띄웁니다 (브라우저 보안 정책상 Google의 응답을 읽을 수 없습니다).
그래서 **6단계 테스트가 중요합니다.** 한 번만 제대로 확인하면 이후로는 안정적으로 동작합니다.

**개인정보** 는 Google Sheet에 모입니다. 선발이 끝나면 시트 공유 범위를 운영진으로 제한하고,
보관 기간이 지나면 삭제하는 편이 좋습니다. 페이지에 그렇게 안내해 두었습니다.
